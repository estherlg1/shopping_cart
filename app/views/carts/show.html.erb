
<h2>Your cart:</h2>

<% @cart.each do |product| %>
  <tr>
    <td><%= product.name %></td>
    <td><%= product.price %></td>
    <%= button_to "Remove from cart", order_line_path, {method: :delete, params: {product_id: product.id}} %>
  </tr>
<% end %>

<%= link_to('back', products_path) %>